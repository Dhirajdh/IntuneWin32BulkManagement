<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manage App Assignments"
        Height="750" Width="920"
        WindowStartupLocation="CenterOwner"
        Background="#F3F6FB"
        FontFamily="Segoe UI"
        ResizeMode="CanResize">

<Window.Resources>

    <!-- ===================== COLORS ===================== -->
    <SolidColorBrush x:Key="AccentBrush" Color="#1F6FEB"/>
    <SolidColorBrush x:Key="AccentHoverBrush" Color="#3A82F2"/>
    <SolidColorBrush x:Key="SuccessBrush" Color="#22C55E"/>
    <SolidColorBrush x:Key="NeutralBorderBrush" Color="#D9DEE6"/>
    <SolidColorBrush x:Key="NeutralBgBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="#1B2430"/>

    <!-- ===================== PRIMARY BUTTON ===================== -->
    <Style x:Key="PrimaryButton" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Height" Value="36"/>
        <Setter Property="Padding" Value="20,0"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="18" SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource AccentHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="bd" Property="Background" Value="#A9C3E8"/>
                            <Setter Property="Foreground" Value="#F0F4FA"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===================== DANGER BUTTON ===================== -->
    <Style x:Key="DangerButton" TargetType="Button">
        <Setter Property="Background" Value="#FFFFFF"/>
        <Setter Property="Foreground" Value="#D64545"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="BorderBrush" Value="#F0C4C4"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Height" Value="36"/>
        <Setter Property="Padding" Value="20,0"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="18" SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#FDEEEE"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="bd" Property="Background" Value="#F5F6F8"/>
                            <Setter Property="Foreground" Value="#E3AFAF"/>
                            <Setter TargetName="bd" Property="BorderBrush" Value="#F2DCDC"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===================== SECONDARY BUTTON ===================== -->
    <Style x:Key="SecondaryButton" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource NeutralBgBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="BorderBrush" Value="{StaticResource NeutralBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Height" Value="36"/>
        <Setter Property="Padding" Value="20,0"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="18" SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#EEF2F8"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="bd" Property="Background" Value="#F5F6F8"/>
                            <Setter Property="Foreground" Value="#B5BBC4"/>
                            <Setter TargetName="bd" Property="BorderBrush" Value="#E5E8ED"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===================== MODERN CHECKBOX ===================== -->
    <Style x:Key="ModernCheckBox" TargetType="CheckBox">
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal">
                        <Border x:Name="bd" Width="20" Height="20" CornerRadius="6" BorderBrush="{StaticResource NeutralBorderBrush}" BorderThickness="1.5" Background="White" VerticalAlignment="Center">
                            <Path x:Name="checkMark" Data="M3,8 L7,12 L13,4" Stroke="White" StrokeThickness="2.2" StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round" Visibility="Collapsed"/>
                        </Border>
                        <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource AccentBrush}"/>
                            <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            <Setter TargetName="checkMark" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===================== ROUNDED COMBO BOX ===================== -->
    <Style x:Key="RoundedComboBox" TargetType="ComboBox">
        <Setter Property="Height" Value="36"/>
        <Setter Property="FontSize" Value="12.5"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton x:Name="ToggleButton" Focusable="false" FocusVisualStyle="{x:Null}"
                                      IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border x:Name="bd" Background="#F7F9FC" BorderBrush="{StaticResource NeutralBorderBrush}"
                                            BorderThickness="1" CornerRadius="18" SnapsToDevicePixels="True">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="28"/>
                                            </Grid.ColumnDefinitions>
                                            <Path Grid.Column="1" Data="M0,0 L4,4 L8,0" Stroke="#6B7787" StrokeThickness="1.5"
                                                  StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round"
                                                  HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                            <Setter TargetName="bd" Property="Background" Value="White"/>
                                        </Trigger>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                            <Setter TargetName="bd" Property="Background" Value="White"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          Margin="14,0,32,0" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                        <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                            <Border MinWidth="{TemplateBinding ActualWidth}" MaxHeight="300"
                                    Background="White" BorderBrush="{StaticResource NeutralBorderBrush}"
                                    BorderThickness="1" CornerRadius="10" Margin="0,4,0,0" SnapsToDevicePixels="True">
                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                    <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBoxItem">
        <Setter Property="Padding" Value="12,7"/>
        <Setter Property="FontSize" Value="12.5"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="bd" Background="Transparent" Padding="{TemplateBinding Padding}">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#F0F7FF"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#E8F0FE"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===================== SEARCH BOX ===================== -->
    <Style x:Key="RoundedSearchBox" TargetType="TextBox">
        <Setter Property="Height" Value="36"/>
        <Setter Property="Padding" Value="14,0,14,0"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="BorderBrush" Value="{StaticResource NeutralBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Background" Value="#F7F9FC"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="18" SnapsToDevicePixels="True">
                        <Grid>
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            <Setter TargetName="bd" Property="Background" Value="White"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</Window.Resources>

<Grid Margin="20">
<Grid.RowDefinitions>
    <RowDefinition Height="Auto"/>
    <RowDefinition Height="Auto"/>
    <RowDefinition Height="Auto"/>
    <RowDefinition Height="*"/>
    <RowDefinition Height="Auto"/>
    <RowDefinition Height="Auto"/>
    <RowDefinition Height="Auto"/>
    <RowDefinition Height="Auto"/>
</Grid.RowDefinitions>

<!-- ===================== HEADER ===================== -->
<Border Grid.Row="0" Margin="0,0,0,12">
    <StackPanel>
        <TextBlock Text="Manage App Assignments" FontSize="18" FontWeight="Bold" Foreground="#1B2430"/>
        <TextBlock Name="txtSelectedAppsInfo" Text="Loading..." FontSize="12" Foreground="#6B7787" Margin="0,4,0,0"/>
    </StackPanel>
</Border>

<!-- ===================== CURRENT ASSIGNMENTS ===================== -->
<Border Grid.Row="1" Margin="0,0,0,12" Background="#F0F7FF" BorderBrush="#BFD7F5" BorderThickness="1" CornerRadius="10" Padding="14,10">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Text="Current:" FontWeight="SemiBold" FontSize="12" Foreground="#1F6FEB" VerticalAlignment="Top" Margin="0,0,10,0"/>
        <TextBlock Grid.Column="1" Name="txtCurrentAssignments" Text="Loading..." FontSize="12" Foreground="#374151" TextWrapping="Wrap"/>
    </Grid>
</Border>

<!-- ===================== SECTION: AVAILABLE GROUPS ===================== -->
<Border Grid.Row="2" Margin="0,0,0,12" Background="White" BorderBrush="#E7EBF1" BorderThickness="1" CornerRadius="10" Padding="16">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Available Entra Groups" FontWeight="SemiBold" Foreground="#1B2430" FontSize="13" Margin="0,0,0,8"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Name="txtGroupSearch" Grid.Column="0" Style="{StaticResource RoundedSearchBox}" Margin="0,0,8,0"/>

            <Button Name="btnLoadGroups" Grid.Column="1" Content="Search Groups" Style="{StaticResource SecondaryButton}" Width="160"/>
        </Grid>
    </Grid>
</Border>

<!-- ===================== GROUPS LIST ===================== -->
<Border Grid.Row="3" Background="White" BorderBrush="#E7EBF1" BorderThickness="1" CornerRadius="10" Padding="2">
    <ListBox Name="lstAvailableGroups" Background="Transparent" BorderThickness="0" ScrollViewer.VerticalScrollBarVisibility="Auto">
        <ListBox.ItemTemplate>
            <DataTemplate>
                <Border Padding="14,10" BorderBrush="#F0F0F0" BorderThickness="0,0,0,1" Margin="0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <CheckBox Grid.Column="0" Style="{StaticResource ModernCheckBox}" IsChecked="{Binding IsSelected}" VerticalAlignment="Center"/>

                        <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="{Binding GroupName}" FontWeight="SemiBold" Foreground="#1B2430" FontSize="13"/>
                            <TextBlock Text="{Binding GroupId}" FontSize="11" Foreground="#6B7787" Margin="0,2,0,0"/>
                        </StackPanel>

                        <Border Grid.Column="2" CornerRadius="8" Background="{Binding StatusColor, FallbackValue=#F0F0F0}" Padding="8,4" VerticalAlignment="Center">
                            <TextBlock Text="{Binding MembershipStatus}" FontSize="11" FontWeight="SemiBold" Foreground="White"/>
                        </Border>
                    </Grid>
                </Border>
            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>
</Border>

<!-- ===================== ASSIGNMENT INTENT ===================== -->
<Border Grid.Row="4" Margin="0,12,0,0" Background="White" BorderBrush="#E7EBF1" BorderThickness="1" CornerRadius="10" Padding="16,12">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Text="Assignment Intent:" FontWeight="SemiBold" Foreground="#1B2430" FontSize="13" VerticalAlignment="Center" Margin="0,0,20,0"/>
        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
            <RadioButton Name="rdoRequired" Content="Required" IsChecked="True" Margin="0,0,24,0" FontSize="13" Foreground="#1B2430" VerticalContentAlignment="Center"/>
            <RadioButton Name="rdoAvailable" Content="Available for Enrolled Devices" Margin="0,0,24,0" FontSize="13" Foreground="#1B2430" VerticalContentAlignment="Center"/>
            <RadioButton Name="rdoUninstall" Content="Uninstall" FontSize="13" Foreground="#1B2430" VerticalContentAlignment="Center"/>
        </StackPanel>
    </Grid>
</Border>

<!-- ===================== SCOPE TAGS ===================== -->
<Border Grid.Row="5" Margin="0,8,0,0" Background="White" BorderBrush="#E7EBF1" BorderThickness="1" CornerRadius="10" Padding="16,12">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Text="Scope Tag:" FontWeight="SemiBold" Foreground="#1B2430" FontSize="13" VerticalAlignment="Center" Margin="0,0,16,0"/>
        <ComboBox Name="cmbScopeTag" Grid.Column="1" Style="{StaticResource RoundedComboBox}" Margin="0,0,16,0"/>
        <Button Name="btnCopyFrom" Grid.Column="2" Content="Copy From App..." Width="140" Style="{StaticResource SecondaryButton}"/>
    </Grid>
</Border>

<!-- ===================== SELECTED SUMMARY ===================== -->
<Border Grid.Row="6" Margin="0,8,0,8" Background="White" BorderBrush="#E7EBF1" BorderThickness="1" CornerRadius="10" Padding="16">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" VerticalAlignment="Center">
            <TextBlock FontWeight="SemiBold" Foreground="#1B2430" FontSize="13">
                <Run Text="Selected Groups: "/>
                <Run Name="txtSelectedGroupCount" Text="0" Foreground="#1F6FEB" FontWeight="Bold"/>
            </TextBlock>
            <TextBlock Name="txtSelectedGroupsList" Text="-" FontSize="11" Foreground="#6B7787" Margin="0,4,0,0" TextWrapping="Wrap"/>
        </StackPanel>

        <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="12,0,0,0">
            <Button Name="btnSelectAll" Content="Select All" Width="110" Margin="0,0,8,0" Style="{StaticResource SecondaryButton}"/>
            <Button Name="btnDeselectAll" Content="Deselect All" Width="130" Style="{StaticResource SecondaryButton}"/>
        </StackPanel>
    </Grid>
</Border>

<!-- ===================== ACTION BUTTONS ===================== -->
<Border Grid.Row="7" Margin="0,0,0,0">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" Name="txtAssignmentStatus" Text="" FontSize="11" Foreground="#6B7787" VerticalAlignment="Center"/>

        <StackPanel Grid.Column="1" Orientation="Horizontal">
            <Button Name="btnRemoveAssignments" Content="Remove Assignments" Width="170" Margin="0,0,12,0" Style="{StaticResource DangerButton}"/>
            <Button Name="btnCancel" Content="Cancel" Width="110" Margin="0,0,12,0" Style="{StaticResource SecondaryButton}"/>
            <Button Name="btnAssignGroups" Content="Assign" Width="110" Style="{StaticResource PrimaryButton}"/>
        </StackPanel>
    </Grid>
</Border>

</Grid>
</Window>
